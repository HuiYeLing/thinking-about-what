#include<bits/stdc++.h>
using namespace std;
class Solution {
public:
    int cherryPickup(vector<vector<int>>& grid) {
        int n,count=0,m;
        int grid[n][n]={0};
        for (int i = 0; i < n; i++)
        {
            for (int j = 0; j < m; j++)
            {
                grid[i][j]=0;
            }
            
        }
        
    }
};